# 轮询（`round-robin`）

## 行为

按照聚合中目标身份的稳定顺序循环选择：第一个请求选择第一个目标，后续请求依次选择下一个目标，到末尾后回到开头。

轮询游标在路由决定时更新，并按具体 target identity 维护。故障转移时，即使失败目标暂时从候选列表消失，也不会因为过滤列表变短而跳过下一个目标。

## 适用场景

- 多个上游能力相近，希望简单均匀分流。
- 希望请求顺序可观察、可复现。

## 注意事项

轮询不读取成本、余量、延迟或并发负载。目标能力差异明显时，优先考虑加权、自动优选或上下文优化。
