# 最少使用（`least-used`）

## 行为

选择具体目标历史路由尝试次数最少的目标。每次尝试都会计数，包括最终失败和故障转移中的中间失败；没有记录的目标按 0 次处理。

该策略使用 target identity 统计，不使用 provider 的当前 `in_flight` 数，因此不会把瞬时并发误当成长期使用量。

## 适用场景

- 希望逐渐填平不同目标之间的历史流量差。
- 目标数量少、请求量稳定，不需要复杂的实时负载模型。

## 注意事项

计数只存在于运行时内存，应用重启后会重新计数。想根据当前并发量选目标，应使用自动优选或 P2C。
