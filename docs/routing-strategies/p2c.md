# 二选一负载均衡（`p2c`）

## 行为

每次随机抽取两个不同的可用目标，分别计算：

- 成功率：成功次数 ÷ 总路由尝试次数。
- 延迟分：根据目标延迟历史计算，延迟越低分越高。

综合分更高的目标获选；分数相同时保留第一次抽到的目标。没有历史数据的目标使用中性默认值，因此新目标仍然有机会被选择。

## 适用场景

- 目标数量较多，希望以很低的计算成本获得比纯随机更好的负载均衡。
- 目标健康度和延迟差异会随时间变化。

## 注意事项

P2C 使用的是具体目标的运行时历史，应用重启后历史会清空；只有一个候选目标时直接选择它。
