# 最近成功路径（`lkgp`）

## 行为

优先选择该聚合最近一次成功请求使用的具体目标。显式 targets 使用 target id；旧版聚合使用 provider/model identity，因此同名模型在不同提供商上仍能区分。

如果最近成功目标已经不可用，则尝试最近成功模型；仍然没有可用匹配时回退到优先级策略。失败请求不会更新“最近成功”记录。

## 适用场景

- 某个上游已经验证稳定，希望后续请求继续复用。
- 希望在不固定配置主目标的情况下保留成功路径。

## 注意事项

最近成功记录只存在于运行时内存，重启后会回到优先级选择。
