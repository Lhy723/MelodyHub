# 顺序流水线（`pipeline`）

## 行为

按目标优先级从高到低依次执行，每个阶段拿到上一个阶段的文本结果，并以“继续原任务”的用户消息形式追加到下一阶段。只有最后一个阶段在原请求为流式时流式返回。

流水线阶段使用当前可用目标；某个阶段失败会终止本次流水线，不会把空结果传给下一阶段。

## 适用场景

- 需要“分析 → 改写 → 审核”这类固定步骤。
- 希望不同模型承担不同阶段，而不是并行投票。

## 注意事项

每增加一个阶段，就会增加一次上游请求和延迟。中间阶段必须返回可提取的文本；如果没有可用阶段或中间阶段没有文本，代理会返回错误。
