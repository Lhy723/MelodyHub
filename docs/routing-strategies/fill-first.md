# 优先用满（`fill-first`）

## 行为

在有有效且大于 0 的 `quotaRemaining` 的目标中，选择优先级最高的目标；因此会尽量把高优先级目标的配额用完，再转向下一个目标。

如果所有目标都没有余量数据，或余量都为 0，则回退到普通优先级选择。余量为 0 是有效数据，不会被当成“未知”。

## 适用场景

- 希望先消耗某个供应商的免费额度或短周期额度。
- 有明确主备顺序，同时希望额度耗尽后自动切换。

## 注意事项

该策略按优先级而不是余量大小选择；如果想直接选择余量最多的目标，应使用最大余量。
