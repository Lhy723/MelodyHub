# 加权（`weighted`）

## 行为

按目标的 `weight` 进行加权随机选择。权重是相对比例，不是百分比；例如 `8:2` 约等于 80% 和 20% 的长期分布。

目标在进入选择器前必须启用且 `weight > 0`。权重不使用历史请求数，也不会因为一次失败自动降低。

## 适用场景

- 两个模型能力相近，但希望按容量或预算分流。
- 灰度发布，例如新目标权重设为 `1`，稳定目标设为 `9`。

## 注意事项

短时间窗口内的实际比例会受到随机波动影响。需要每个目标至少使用一次再重复时，应改用无重复随机。
