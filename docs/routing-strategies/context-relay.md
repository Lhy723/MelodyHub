# 上下文接力（`context-relay`）

## 行为

根据聚合名称、策略和请求前缀生成亲和键，并为同一个亲和键记住目标。相同会话或相同前缀的后续请求会优先回到同一个目标。

如果原目标不可用，路由会在当前候选中重新建立一个稳定的亲和分配；客户端携带的完整上下文仍会照常转发。

## 适用场景

- 需要把同一会话尽量固定到同一个模型。
- 上游有会话缓存、KV 缓存或本地上下文状态。

## 注意事项

亲和映射属于运行时状态，应用重启后会重新建立。它不在服务端保存会话内容，也不替客户端补齐缺失的历史消息。
