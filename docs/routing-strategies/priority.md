# 优先级（`priority`）

## 行为

从所有可用目标中选择 `priority` 数值最大的目标。相同优先级时保持配置顺序。

请求失败后，失败提供商会被排除，路由重新在剩余目标中选择最高优先级目标。

## 适用场景

- 一个主模型配多个备用模型。
- 希望路由结果完全可预测。
- 不需要根据成本、配额或历史负载动态调整。

## 配置建议

把主目标设置为较大的优先级，例如 `100`，备用目标依次使用 `50`、`10`。如果希望所有目标都参与分流，应改用轮询、加权或随机策略。
