# 最大余量（`headroom`）

## 行为

在有有效 `quotaRemaining` 的候选中，选择剩余配额比例最高的目标。UI 以百分比编辑，内部按 0–1 的比例保存；0% 是有效余量值。

如果所有目标都没有有效余量数据，则回退到优先级策略，而不是把未知值当成 0 或负数参与比较。

## 适用场景

- 希望把请求发送到当前可用额度最多的供应商。
- 供应商配额是主要瓶颈，成本和延迟不是首要因素。

## 注意事项

最大余量依赖目标上的配置或遥测提示，代理不会自动查询上游账单。若要优先“用满”高优先级目标，应使用优先用满。
