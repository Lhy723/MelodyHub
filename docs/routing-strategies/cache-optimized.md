# 缓存优化（`cache-optimized`）

## 行为

根据请求前缀生成亲和键，并把同一前缀固定到同一目标，以提高上游 Prompt Cache 或前缀缓存的命中机会。亲和映射按聚合和策略隔离。

目标不可用时会在当前候选中重新建立映射。策略不读取上游缓存命中率，也不缓存请求内容本身。

## 适用场景

- 系统提示词或长前缀经常重复。
- 上游按目标维护 Prompt Cache，跨目标会损失缓存命中。

## 注意事项

它与上下文接力使用同一类运行时亲和机制，但目标不同：缓存优化关注前缀命中，接力关注会话连续性。亲和映射重启后会清空。
