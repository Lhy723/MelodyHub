# 无重复随机（`strict-random`）

## 行为

为每个聚合维护一个随机牌堆：一轮内每个当前候选目标只会出现一次，所有目标使用完后重新洗牌。牌堆顺序由聚合、目标身份和轮次生成，因此同一轮不会因并发请求读到相同游标。

## 适用场景

- 希望每个目标在一轮内都得到机会。
- 不想像普通随机那样连续命中同一个目标。

## 注意事项

候选目标因健康状态或故障转移而变化时，当前请求只会从剩余候选中选择；它不是跨重启持久化的全局随机序列。
