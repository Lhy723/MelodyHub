# 重置窗口（`reset-window`）

## 行为

选择 `quotaResetAt` 时间戳最早的目标，即优先使用最接近配额重置的窗口。没有重置时间的目标不参与比较。

如果没有任何目标配置重置时间，则回退到优先级策略。

## 适用场景

- 额度即将重置，想先消耗临近窗口。
- 供应商的余量数字不可靠，但重置时间比较明确。

## 注意事项

该策略不比较剩余比例；一个只剩 1% 但马上重置的目标，仍可能优先于余量更高但很久才重置的目标。
