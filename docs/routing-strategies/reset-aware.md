# 重置感知（`reset-aware`）

## 行为

为每个目标计算一个综合分：

```text
score = quotaRemaining / sqrt(hoursToReset)
```

它倾向于选择余量较满、且配额窗口较快重置的目标，从而优先消耗即将重置的额度。没有余量时使用 50% 的中性默认值，没有重置时间时使用 7 天的中性默认窗口。

## 适用场景

- 多个供应商有不同的配额周期。
- 希望在额度重置前优先使用即将失效的余额。

## 注意事项

这不是“只选最早重置”的策略；如果更关心重置时间本身，应使用重置窗口。如果只关心余额大小，应使用最大余量。
